{
  "name": "rbx-asset-uploader",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "verify": "bash scripts/smoke-test.sh",
    "check": "cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings && npm run verify"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
