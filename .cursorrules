# rbx-asset-uploader

## 検証・デバッグ

- 変更後は必ず `npm run verify` で起動確認する
- 失敗時は `RUST_BACKTRACE=1 npm run verify` または `bash scripts/debug.sh` でパニック原因を特定
- ログ: `.verify.log`, `.debug.log`

## 構成

- Tauri v2 + Vanilla JS
- Rust: src-tauri/src/
- HTTP サーバー: axum, port 58750
